install.packages("tidyverse")
install.packages("patchwork")
install.packages("gridExtra")
qpois?
w
?qpois
lambda  <- 1000
p       <- 0.01
#p      <- 0.05
#p      <- 0.2
nr_zile <- 365
qpois(p, lambda)
qpois(p, lambda)
qpois(p, lambda)
set.seed(100)
qpois(p, lambda)
set.seed(101)
qpois(p, lambda)
set.seed(67)
lambda  <- 1000
p       <- 0.01
#p      <- 0.05
#p      <- 0.2
nr_zile <- 365
total_cereri < rpois(nr_zile, lambda)
set.seed(67)
lambda  <- 1000
p       <- 0.01
#p      <- 0.05
#p      <- 0.2
nr_zile <- 365
total_cereri <- rpois(nr_zile, lambda)
total_cereri
set.seed(87)
lambda  <- 1000
p       <- 0.01
#p      <- 0.05
#p      <- 0.2
nr_zile <- 365
total_cereri <- rpois(nr_zile, lambda)
total_cereri
?binom()
?rbinom
cereri_sus <- rbinom(
n = nr_zile,
size = total_cereri,
prob = p
)
cereri_sus
total_cereri[:10]
total_cereri[1:10]
cereri_sus[1:10]
date <- data.frame(
zi = 1:nr_zile,
total = total_cereri,
normale = total_cereri - cereri_sus,
sus = cereri_sus
)
datew
date
?hist
hist(date$sus)
hist(date$sus, breaks = 15)
hist(date$sus, breaks = 50)
hist(date$sus, breaks = 20)
hist(date$sus,
col = "skyblue",
border = "white",
main = "Histograma date sus",
xlab = "Nr. sus",
ylab = "Frecventa"
breaks = 20
hist(date$sus,
col = "skyblue",
border = "white",
main = "Histograma date sus",
xlab = "Nr. sus",
ylab = "Frecventa",
breaks = 20
)
hist(date$sus,
col = "skyblue",
border = "gray",
main = "Histograma date sus",
xlab = "Nr. sus",
ylab = "Frecventa",
breaks = 20
)
hist(date$sus,
col = "skyblue",
border = "black",
main = "Histograma date sus",
xlab = "Nr. sus",
ylab = "Frecventa",
breaks = 20
)
?sample
sample(date$total, size = date$total * proc_verificare,
replace = FALSE,
p = date$sus / date$total # nr caz fav / total
)
proc_verificare <- 10 / 100
sample(date$total, size = date$total * proc_verificare,
replace = FALSE,
p = date$sus / date$total # nr caz fav / total
)
date$total * proc_verificare
sample(date$total, size = as.integer(date$total * proc_verificare),
replace = FALSE,
p = date$sus / date$total # nr caz fav / total
)
size = as.integer(date$total * proc_verificare)
size
clear
?rhyper
date$verificate <- floor(date$total * proc_verificare)
date%verificate[1:15]
date$verificate[1:15]
date$detectate <- rhyper(
nn <- nr_zile,
m = date$sus, # N2
n = date$normale, #N1
k = date$verificate # actual nr verificate
)
date$detectate[1:100]
date
date$nedetectate <- date$sus - date$detectate
head(date)
max(date$total)
min(date%total)
min(date$total)
head(date)
data$detectate > 0
date$detectate > 0
head(date$detectate > 0)
vector_logic <- date$detectate > 0
length(vector_logic == TRUE) / length(vector_logic)
vector_logic == TRUE
vector_logic <- date$detectate > 0
sum(vector_logic) / length(vector_logic)
mean(date$detectate)
mean(date$detectate > 0)
mean(date$detectate)
mean(date$nedetectate)
prop_detectate <- date$detectate / date$total
mean(prop_detectate)
mean(date$detectate / date$sus)
mean(date$nedetectate / date$sus)
mean(date$verificate)
mean(date$detectate / date$sus) / mean(date$verificate)
mean(date$detectate / date$sus) / mean(date$verificate) * 100
mean(date$detectate) / mean(date$verificate) * 100
set.seed(87)
lambda  <- 1000 # PARAMETRIZABIL
p       <- 0.01 # PARAMETRIZABIL
#p      <- 0.05
#p      <- 0.2
nr_zile <- 365
total_cereri <- rpois(nr_zile, lambda)
cereri_sus <- rbinom(
n = nr_zile,
size = total_cereri,
prob = p
)
date <- data.frame(
zi = 1:nr_zile,
total = total_cereri,
normale = total_cereri - cereri_sus,
sus = cereri_sus
)
20 / 100 * date$total - lambda
0 / 100 * (date$total - lambda)
20 / 100 * (date$total - lambda)
20 / 100 + (date$total - lambda)/10
proc_adapt <- 20 / 100 + (date$total - lambda)/10
date$verificate <- floor(date$total * proc_adapt)
date$verificate
proc_adapt <- 20 / 100 + (date$total - lambda)/100
proc_adapt <- 20 / 100 + (date$total - lambda)/100
date$verificate <- floor(date$total * proc_adapt)
proc_adapt
date$verificate
proc_adapt <- 20 / 100 + (date$total - lambda)/lambda
date$verificate <- floor(date$total * proc_adapt)
date$verificate
head(data)
head(date)
proc_adapt <- 20 / 100 + (date$total - lambda)/lambda
date$verificate <- floor(date$total * proc_adapt)
vector_logic <- date$detectate > 0
prob_empiric <- sum(vector_logic) / length(vector_logic)
date
proc_adapt <- 20 / 100 + (date$total - lambda)/lambda
date$verificate <- floor(date$total * proc_adapt)
date$detectate <- rhyper(
nn <- nr_zile,
m = date$sus, # N2
n = date$normale, #N1
k = date$verificate # actual nr verificate
)
date$nedetectate <- date$sus - date$detectate
vector_logic <- date$detectate > 0
prob_empiric <- sum(vector_logic) / length(vector_logic)
# alternativ mean(date$detectate > 0)
# b)
prob_empiric
head(date)
mean(date$detectate / date$sus)
mean(date$nedetectate / date$sus)
mean(date$verificate)
mean(date$detectate) / mean(date$verificate) * 100
library(shiny); runApp('C:/Users/Mircea/Desktop/Proiect-Statistica-R/src/problema2.r')
runApp('C:/Users/Mircea/Desktop/Proiect-Statistica-R/src/problema2.r')
library(shiny); runApp('C:/Users/Mircea/Desktop/Proiect-Statistica-R/src/problema2.r')
